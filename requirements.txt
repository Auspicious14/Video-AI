# ── Core web framework ────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9       # multipart/form-data for image upload
pydantic==2.7.1
python-dotenv==1.0.1

# ── HTTP clients ──────────────────────────────────────────────────────────────
httpx==0.27.0                  # async HTTP (HuggingFace AI video API calls)
aiohttp                        # required by AsyncInferenceClient

# ── AI — Script generation ────────────────────────────────────────────────────
google-genai
# ── AI — Image generation ──────────────────────────────────────────────────────
huggingface_hub==0.30.1        # AsyncInferenceClient: FLUX.1-schnell images
Pillow==10.3.0                 # PIL — text overlays, JPEG conversion

# ── AI — Audio / TTS ─────────────────────────────────────────────────────────
gTTS==2.5.1                    # free text-to-speech (no key needed)
# elevenlabs==1.50.6           # premium TTS — uncomment when upgrading

# ── Video rendering ───────────────────────────────────────────────────────────
# FFmpeg must be installed on the system:
#   macOS:  brew install ffmpeg
#   Ubuntu: apt install ffmpeg
# No Python package needed — called via subprocess.
gradio_client>=0.9.0 # SadTalker + LatentSync HF Spaces caller