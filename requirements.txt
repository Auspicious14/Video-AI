# ── Core web framework ────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.10.0
python-dotenv>=1.0.0

# ── HTTP clients ──────────────────────────────────────────────────────────────
httpx>=0.27.0                  # async HTTP (HuggingFace AI video API calls)
aiohttp                        # required by AsyncInferenceClient

# ── AI — Script generation ────────────────────────────────────────────────────
google-genai                   # Gemini SDK (fallback provider + vision)
openai>=1.30.0                 # OpenAI-compatible SDK — used for Groq (primary provider)
python-json-logger>=2.0.7      # Structured JSON logging for the AI layer
# ── AI — Image generation ──────────────────────────────────────────────────────
huggingface_hub>=0.30.0        # AsyncInferenceClient: FLUX.1-schnell images (fallback)
fal-client                      # fal.ai API for FLUX.1-dev (primary)
Pillow>=11.0.0                 # PIL — text overlays, JPEG conversion (supports Python 3.14)
requests                        # For downloading images from fal.ai

# ── AI — Audio / TTS ─────────────────────────────────────────────────────────
gTTS>=2.5.0                     # free text-to-speech (fallback)
pykokoro>=0.6.0                 # high-quality TTS (supports Python 3.10-3.13)
soundfile                       # WAV file handling
numpy                           # array operations for audio
torch                           # required for pykokoro (optional but recommended
# elevenlabs==1.50.6           # premium TTS — uncomment when upgrading

# ── Video rendering ───────────────────────────────────────────────────────────
# FFmpeg must be installed on the system:
#   macOS:  brew install ffmpeg
#   Ubuntu: apt install ffmpeg
# No Python package needed — called via subprocess.
gradio_client>=0.9.0 # SadTalker + LatentSync HF Spaces caller
requests
